<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Style TargetType="{x:Type TextBlock}">
        <Setter Property="TextWrapping" Value="NoWrap"/>
        <Setter Property="TextTrimming" Value="None"/>
        <Setter Property="Background" Value="{DynamicResource ControlNormalBrushKey}"/>
        <Setter Property="Foreground" Value="{DynamicResource ForegroundNormalBrushKey}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource ControlMouseOverBrushKey}"/>
                <Setter Property="Foreground" Value="{DynamicResource ForegroundMouseOverBrushKey}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{DynamicResource ControlDisabledBrushKey}"/>
                <Setter Property="Foreground" Value="{DynamicResource ForegroundDisbledBrushKey}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>